
/** 
 * The below is our REST APIs to perform CRUD on a password item.
 * Each of these api requires user to be logged in before they can proceed.
 * Hence, each api will use 'withAuth' custom middleware that will redirect 
 * user to /login if not logged in.
 */ 
 


//POST(/) 
//PUT(/:id) 
//DELETE(/:id)
//GET(/)